{% extends 'base.html.twig' %}
{% block body %}
    <div id="wrapper">
        <h3>My lists</h3>
        <div id="container">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
        <button><a href="{{ path('to_do_lists') }}">Cancel</a></button>
    </div>
{% endblock %}